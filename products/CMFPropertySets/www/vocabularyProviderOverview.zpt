<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      xmlns:i18n="http://xml.zope.org/namespaces/i18n"
      tal:omit-tag="">
  <head tal:replace="nothing"><title></title></head>
  <body tal:omit-tag="">
    <h1 tal:replace="structure here/manage_page_header">Header</h1>

    <h2 tal:define="manage_tabs_message request/manage_tabs_message | options/manage_tabs_message | nothing"
        tal:replace="structure here/manage_tabs">Tabs</h2>

    <h2 tal:define="form_title string:Vocabulary Provider Overview"
        tal:replace="structure here/manage_form_title">
      Form Title
    </h2>

    <p class="form-help">
      A Vocabulary Provider is a pluggable component that provides a
      specialized context-based vocabulary for Plone Desktop.
    </p>

    <p class="form-help">
      More specifically, a Vocabulary Provider provides a set of
      values for a (namespace, property) combo.
    </p>

    <p class="form-help">
      There are two kinds of Vocabulary Providers: Global and
      Local. Global ones provide site-wide vocabulary information,
      which is the same to all contained objects. Local providers
      provide information specific about the object passed to them as
      'context'.
    </p>

    <h3>Summary</h3>

    <dl class="form-help">
      <dt class="form-label">
        Identifier
      </dt>
      <dd tal:content="context/id">
        id
      </dd>
      <dt class="form-label">
        Namespace
      </dt>
      <dd tal:content="context/ns">
        ns
      </dd>
      <dt class="form-label">
        Property Id
      </dt>
      <dd tal:content="context/propid">
        propid
      </dd>
    </dl>

    <h1 tal:replace="structure here/manage_page_footer">Footer</h1>
  </body>
</html>